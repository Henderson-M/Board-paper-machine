BPM reminder — packs run (Mon / Thu)

Time to run the Board Paper Machine packs check.

How: open Claude Code in the "My assistant" folder and say:  run BPM

What it does: checks the detection window (prev 2 days -> next 10 days) for new board packs, analyses them, and sends papers alerts to the relevant correspondents. Skips the full date scan (only needed monthly).

Tip: it dedupes against state, so anything already alerted won't resend.

— Board paper machine (scheduled reminder)
